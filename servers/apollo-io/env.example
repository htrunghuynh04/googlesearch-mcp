# Apollo.io API Key
# Get your API key from https://www.apollo.io/api
# For local development, set your API key here
# For production (Docker), API key should be passed via HTTP header X-Apollo-Api-Key
APOLLO_IO_API_KEY=your_apollo_api_key_here

# Server port (default: 8080)
PORT=8080
